/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.imgur.com', 'i5.walmartimages.com', 'images.unsplash.com'],
    },
  };
  
  export default nextConfig;
  